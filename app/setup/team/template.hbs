{{spd-nav preventContinueMessage=preventContinueMessage}}

<div class="setup-step">
  <h1 class="setup-step-title">Team Members</h1>

  <nav class="panel-nav">
    <ul class="tabs">
      <li class="{{if (eq panel 'team') 'active' null}}">
        <a {{action 'setPanel' 'team'}}>Team <span class="badge">14</span></a>
      </li>
      <li class="{{if (eq panel 'invites') 'active' null}}">
        <a {{action 'setPanel' 'invites'}}>Pending Invitations <span class="badge">4</span></a>
      </li>
    </ul>
  </nav>

  <div class="panels">
    {{#if (eq panel 'team')}}
      {{team-index}}
    {{/if}}

    {{#if (eq panel 'invites')}}
      {{invite-index}}
    {{/if}}
  </div>

  <div class="panel panel-striped">
    <div class="panel-section">
      <div class="row">
        <div class="col-xs-8">
          <div class="panel-section-title">Invite your entire workforce</div>
          <p>Lorem ipsum dolor sit amet, consectetur adipiscing elit. Donec aliquet purus ornare condimentum malesuada. Pellentesque diam mi, fermentum ut sapien eu, vehicula dictum elit. Integer cursus sagittis ullamcorper.
          </p>
        </div>
        <div class="col-xs-4">
          <button class="btn btn-xl btn-primary">Invite more users</button>
        </div>
      </div>
    </div>
  </div>

  <div>
    {{link-to "Continue →" 'setup.data-environments' class="btn btn-primary btn-lg"}}
  </div>
</div>
