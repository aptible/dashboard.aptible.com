<header>
  <div class="container">
    <h1 class="dashboard-header">{{model.organization.name}}</h1>
  </div>
</header>


<div class="container">
  {{#if model.alerts}}
    <section class="compliance-section">
      <header>
        <span class="header-title">Compliance Alerts</span>
        <span class="badge">{{model.alerts.length}}</span>
      </header>

      <div class="row">
        <div class="col-xs-12">
          {{compliance-alerts alerts=model.alerts}}
        </div>
      </div>
    </section>
  {{/if}}

  <section class="compliance-section">
    <header>
      <span class="header-title">Compliance Engines</span>
    </header>

    <div class="row">
      <div class="col-xs-6">
        {{#link-to 'risk-assessments' class="panel-link"}}
          {{compliance-engine-status
            criterion=model.riskCriterion
            criterionDocuments=model.riskAssessmentDocuments
            productionApps=model.productionApps
            users=model.users}}
        {{/link-to}}
      </div>

      <div class="col-xs-6">
        {{#link-to-aptible 'compliance' path='policy' class="panel-link"}}
          {{compliance-engine-status
            criterion=model.policyCriterion
            criterionDocuments=model.policyManualDocuments
            productionApps=model.productionApps
            users=model.users}}
        {{/link-to-aptible}}
      </div>
    </div>

    <div class="row">
      <div class="col-xs-6">
        {{#link-to-aptible app='compliance' path='security' class="panel-link"}}
          {{compliance-engine-status
            criterion=model.appSecurityCriterion
            criterionDocuments=model.appSecurityDocuments
            organization=model.organization
            productionApps=model.productionApps
            users=model.users}}
          {{/link-to-aptible}}
      </div>

      <div class="col-xs-6">
        {{#link-to 'training' class="panel-link" class="panel-link"}}
          {{compliance-engine-status
            criterion=model.trainingCriterion
            criterionDocuments=model.trainingDocuments
            users=model.users
            organization=model.organization
            productionApps=model.productionApps}}
        {{/link-to}}
      </div>
    </div>
  </section>

  {{compliance-dashboard-nav}}
</div>
