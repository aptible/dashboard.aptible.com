{{#if showVerificationBanner}}
  {{#bs-alert alert="warning" as |component|}}
    {{bs-alert-dismiss target=component}}

    Hey {{user.name}}, please verify your email address.
    Check {{user.email}} for verification instructions.

    <button class='btn btn-default btn-xs' {{action 'resendVerification'}} disabled={{isDisabled}}>
      {{#if sending}}
        Sending
      {{else if sent}}
        Verification email sent
      {{else}}
        <i class='fa fa-refresh'></i>
        Resend verification email
      {{/if}}
    </button>

    {{#if error}}
      There was an error sending the email, please try again.
    {{/if}}
  {{/bs-alert}}
{{/if}}
