<div class="resource-list-grid">
  <div class="row">
    {{#each model as |app|}}
      <div class="col-xs-4">
        {{partial 'stack/app'}}
      </div>
    {{/each}}
  </div>

  <div class="resource-actions">
    {{#aptible-ability scope="manage" user=session.currentUser stack=stack as |hasAbility|}}
      {{#if hasAbility}}
        {{link-to 'Create App' 'apps.new' class="btn btn-primary"}}
      {{/if}}
    {{/aptible-ability}}
  </div>
</div>
