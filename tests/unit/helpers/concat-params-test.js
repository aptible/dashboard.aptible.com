import {
  concatParams
} from '../../../helpers/concat-params';
import { module, test } from 'qunit';

module('ConcatParamsHelper');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = concatParams([42]);
  assert.ok(result);
});
